import tkinter as tk

def _init_(self):
    # create a window
    window = Tk()
    window.title("Loan Calculator")

    #create the input boxes.
    Label(window, text = "Annual Interest Rate").grid(row )
